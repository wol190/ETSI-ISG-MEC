description: Parameters related to use of OAuth 2.0
required:
- grantTypes
- tokenEndpoint
properties:
  grantTypes:
    $ref: '../definitions/GrantTypes.yaml'
  tokenEndpoint:
    $ref: '../definitions/TokenEndpoint.yaml'